       // the variables hours, minutes, currentDate, currentTime, currentPeriodNumber, dayNumber,

       // below code will print date in html element with date id
        document.getElementById('date').textContent = currentDate;

       // below code will print time in html element with time id
        document.getElementById('time').textContent = currentTime;

       // below code will print periodName in html element with date id
       document.getElementById('period').textContent = periodName;

        

        document.getElementById('totalDays').textContent = totalDays;
        document.getElementById('present').textContent = present;
        document.getElementById('absent').textContent = absent;
